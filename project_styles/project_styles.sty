% PRINT VERSION

% ========== ОСНОВНЫЕ НАСТРОЙКИ ДОКУМЕНТА ==========

% От этого зависят правильные переносы в словах.
\usepackage[english]{babel}

% кодировка по-умолчанию.
% ВНИМАНИЕ, параметр {inputenc} должен использоваться в документе только один раз
\RequirePackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage{pxfonts}

% ========== НАСТРОЙКИ ЗАГОЛОВКОВ ==========

% Сделал линию под каждым section & subsection
\usepackage{titlesec}

	\titleformat{\section}
	{\normalfont\Large\bfseries}{\thesection)}{1em}{}[{\titlerule[0.1pt]}]

	\titleformat{\subsection}
	{\normalfont\Large\bfseries}{\thesubsection)}{1em}{}[{\titlerule[0.1pt]}]

	\titleformat{\subsubsection}
	{\normalfont\Large\bfseries}{\thesubsubsection)}{1em}{}[{\titlerule[0.1pt]}]

	% Задушил возможность выводить каждый \section с новой страницы
	% \usepackage{titlesec}
	% \newcommand{\sectionbreak}{\clearpage}

% ========== НАСТРОЙКИ АБЗАЦЕВ ==========

% To get a straight right margin in the output, LaTeX inserts varying amounts of space between the words. By default, it also inserts slightly more space at the end of a sentence. However, the extra space added at the end of sentences is generally considered typographically old-fashioned in English language printing. The additional space after periods can be disabled with the command
\frenchspacing

% убираем отступ для красной строки в абзаце
\parindent=0em

% принудительно увеличенный интервал между абзацами.
\parskip=0.4em

% принудительно увеличенный интервал между строчками, чтобы повысить читаемость, бо сливались
\renewcommand{\baselinestretch}{1.2}

% Непонятно что, но `для лучшего выравнивания текста`
\sloppy

% Перечеркивание текста — \sout{Hello world}
% Обязательна опция [normalem], иначе ulem превратит ВСЕ \emph{} в простой подчеркнутый текст
\usepackage[normalem]{ulem}

% ========== СОБСТВЕННЫЕ СТИЛИ ==========

\usepackage{textcomp}

% Решение заебучего отсутствия пробела после закрывающей французской кавычки: текст, который надо взять в кавычки, берем в \russianq{}
\newcommand{\russianq}[1]{\guillemotleft{#1}\guillemotright}

% Обрамление текста ординарными, идеально ровными кавычками (зависят от \usepackage{textcomp})
\newcommand{\ordinaryq}[1]{\textquotesingle {#1}\textquotesingle}

% Обрамление текста английскими кавычками (зависят от \usepackage{textcomp})
\newcommand{\englishq}[1]{``{#1}''}

% Обрамление текста квадратными скобками
\newcommand{\squaredBrackets}[1]{\lbrack{#1}\rbrack}

% ========== НАСТРОЙКИ ОСНОВНЫХ СВОЙСТВ ДОКУМЕНТА ==========

\title{\Huge{Software Testing Glossary}}

\author{{\textbf{Alex\'ei Lup\'an}}}

% Управление расстоянием между строками в tableofcontents
\usepackage{tocloft}
 
\setlength\cftparskip{-1pt}
\setlength\cftbeforesecskip{1pt}
	% change the vertical spacing between the title "Table of Contents" and the first entry
\setlength\cftaftertoctitleskip{15pt}

% ========== НАСТРОЙКИ ИНДЕКСА ТЕРМИНОВ ==========

\usepackage{imakeidx} % Normal LaTeX indexing

% Уменьшил размер всех вхождений в индекс (шрифт чуть меньше общего)
\indexsetup{othercode=\small}

% включил возможность создания индекса терминов.
\makeindex[
% program=makeindex,
title=Index,
program=xindy,
columns=1, % можно =2 для двух колонок, но могут возникнуть сложности с переносами.
columnseprule=true,
noautomatic=false,
intoc=true, % "Предметный указатель" отражается в общем содержимом документа.
options=-C utf8 -s project_styles/index_style.ist]
% options={-s index_style.ist -C utf8 -M multilingual.xdy}]

% https://tex.stackexchange.com/questions/8351/what-do-makeatletter-and-makeatother-do
	\makeatletter
		% Все индексовые вхождения писать только так: \rindex{\textbf{W}!Web}
		% Тогда они будут аккуратно обрабатываться и сортироваться сперва по английскому, а затем по русскому алфавиту.
		\newcommand{\rindex}[2][\imki@jobname]{\index[#1]{\detokenize{#2}}}

		% Выравниванием термины в колонках по левому краю
		\def\@idxitem{\par\hangindent 0pt}
	\makeatother

% ========== НАСТРОЙКИ КАРТИНОК ==========

% Возможность конвертировать eps картинки в pdf
\usepackage{graphicx}

% a Perl script that con­verts an EPS file to an ‘en­cap­su­lated’ PDF file (a sin­gle page file whose me­dia box is the same as the orig­i­nal EPS’s bound­ing box). The re­sult­ing file suit­able for in­clu­sion by pdfTeX as an im­age.
\usepackage{epstopdf}

% Возможность выравнивать картинки (обтекание текстом)
\usepackage{wrapfig}

% ========== НАСТРОЙКИ РАЗМЕРА СТРАНИЦ ==========

% Установил более широкое поле для текста, нежели для обычной книги
% Вместо inchi (дюймы) можно использовать cm (сантиметры)
% \usepackage[a4paper, total={6in, 8in}]{geometry}
\usepackage[left=2.5cm, right=2.5cm]{geometry}

% ========== НАСТРОЙКИ ОСТАЛЬНЫХ СВОЙСТВ ДОКУМЕНТА ==========

% ужимаем готовый pdf
\pdfcompresslevel=5

% чтобы работал поиск по PDF
\usepackage{cmap} 

% you have to put \usepackage[pdftex]{hyperref} 
% as the last command into the preamble of your document.
% Параметр colorlinks=true убирает рамки вокруг ссылок, превращая ссылки в окрашенный текст. Догадаться о том, что его надо перевести в режим true, блять, невозможно.
\usepackage[pdftex,colorlinks=true]{hyperref}

\usepackage{todo}
